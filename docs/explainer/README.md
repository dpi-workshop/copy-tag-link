# CopyTagLink Explainer R2 Package

Upload this folder as static files. Open `index.html` to play the HTML-native video.

The video uses relative paths only, so the folder can be zipped, moved, hosted on R2, or served by any static web server.
